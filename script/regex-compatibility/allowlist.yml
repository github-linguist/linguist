---
# yamllint disable rule:line-length
version: 1
# Each exception is bound to its engine, location, composition, and source bytes.
# Changing, moving, or fixing a pattern makes its entry stale.
exceptions: []
