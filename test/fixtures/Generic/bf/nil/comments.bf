namespace Routing
{
    /*
        > input : output <
        ^                 v
        < < < < < < < < < <
    */
    class Direction
    {
        // >:1-:v
        // ^    <
        public const int Step = 1;
    }
}
