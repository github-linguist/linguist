#!/bin/apl -s
⍝lamps and hashes are the same in GNU APL https://en.wikipedia.org/wiki/APL_syntax_and_symbols#Miscellaneous
⍝github isn't counting lanmps
⍝its a unicode glyph.
'This code only has one line of code, but the Line count is not reflecting this.'
