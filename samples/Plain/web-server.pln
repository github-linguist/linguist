// Plain v1.0.0 — Web server (Express shorthand)

web app

route "/"
    reply "Hello from Plain v1.0!"
done

route "/api/status"
    reply json
        status is "ok"
        version is "1.0.0"
        language is "Plain"
    done
done

start 3000
