#obtener el mayor de dos numeros
#condicion con SI normal
a = 5
b = 10
si (a > 10)
  c = a
sino
  c = b
fin
escribir("el mayor es: " . c)

#obtener el menor de dos numeros
#con operador condicional ternario (cond ? exp1 : exp2 )
#c = a < 10 ? a : b     #sin parentesis tambien es correcto
c = (a < 10) ? a : b
escribir("el menor es: " . c)
