module default {
    abstract type HasName {
        required name: str;
        index on (.name);
    }

    abstract type HasEmail {
        required email: str {
            constraint exclusive;
        }
    }

    type User extending HasName, HasEmail {
        multi friends: User;
    }
}
